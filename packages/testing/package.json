{
  "name": "testing",
  "version": "1.0.0",
  "description": "This document will guide you through the process of setting up a new Playwright test project, and authoring and running end-to-end tests for the Contoso web application.",
  "main": "index.js",
  "scripts": {
    "env": "azd env get-values --no-prompt > .env",
    "pretest": "bash ./get-env-vars.sh",
    "test": "playwright test --browser=all --reporter=html",
    "docker:prestart": "npm run pretest", 
    "docker:run": "docker run -v ${PWD}:/app playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.31.1",
    "@types/node": "^18.14.5"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
